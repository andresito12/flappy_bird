/*#include "Pipe.h"

class Pipe {
private:
	float level_position = ofGetWindowWidth();
	float vert_pipe_space = 365.0f;
}
	level_position -= 0.10f;
	if (level_position > 0) {
		level_position -= section_width;
		pipes.pop_front();
		int i = rand() % (ofGetWindowHeight() - 20);
		pipes.push_back(i);
	}
};*/